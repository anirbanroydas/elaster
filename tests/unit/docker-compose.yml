version: "3"

services:

  elaster_unit_tester:
    build: ../..
    image: aroyd/elaster_unit_tester:${ELASTER_UNIT_TEST_IMAGE_TAG:-latest}
    environment:
      ENV: UNIT_TEST 
    # volumes:
    #   - .:/project


